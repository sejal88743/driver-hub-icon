{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for the AI agent feature. Get it from https://aistudio.google.com/apikey. The app boots with a dev placeholder, but the AI agent needs a real key to function.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
